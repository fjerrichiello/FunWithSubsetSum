If the executable is not there compile by using the make file by typing "make"


Once the executable is there 
To run it do "./ssum <TARGET> <   <INPUT-FILE>"

If no input file

The cmd-line argument is the target sum itself.

The program reads the elements a[1]...a[N] from std-input.
